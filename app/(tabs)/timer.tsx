import React from "react";
import { View } from "react-native";

// This is a placeholder screen for the timer button.
// It will not be visible because the button has a custom action.
export default function TimerScreen() {
  return <View />;
}
