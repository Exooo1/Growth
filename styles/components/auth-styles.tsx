import { StyleSheet } from "react-native";

export const authStyles = StyleSheet.create({
  textInputCode: {
    width: "15%",
    aspectRatio: 1,
    borderRadius: 10,
    textAlign: "center",
    fontSize: 18,
    color: "white",
  },
});
